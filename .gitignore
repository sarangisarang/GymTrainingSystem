.env.local
__pycache__/
*.pyc
.venv/
gym.db
node_modules/
.next/
backend/.env
# Root .env is generated by the local docker setup and holds real credentials
# (DB password, JWT secret, Gemini key). It was untracked but not ignored, so a
# single `git add -A` would have published it.
.env
chroma_db/
backend/chroma_db/

# Code-quality tooling artifacts (Issue #31)
.coverage
backend/.coverage
htmlcov/
.ruff_cache/
backend/.ruff_cache/
.pre-commit-cache/
